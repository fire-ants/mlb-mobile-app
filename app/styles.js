const imageWidth = (window.width/3)+30;
const imageHeight = window.width/3;

export const appStyle = {
  resultText: {
    backgroundColor: '#000',
    color: '#FFF',
    height: 20,
  },
  resultImage: {
    height:150,
    width:150,
  }

}
